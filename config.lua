-- jx-fps configuration
-- Created by JamX, QC
-- Licensed under GNU General Public License v3.0

Config = Config or {}

-- Language Options: 'en' (English) or 'fr' (French)
Config.Locale = 'en'

-- Auto‑restore framework
-- "auto"   → QB (QBCore / QBox) + ESX
-- "qb"     → only QBCore / QBox
-- "esx"    → only ESX
-- "none"   → no auto‑restore (player must use /fps)
Config.Framework = 'auto'
